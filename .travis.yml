language: cpp
sudo: false

addons:
  apt:
    packages:
      - libeigen3-dev
      - cpp-coveralls

script:
- ./setup --enable-tests --coverage
- cd build
- make
- cd tests
- ./unit_tests.x
- cd ../..
- coveralls --exclude tests --gcov-options '\-lp'

