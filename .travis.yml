language: cpp
sudo: false

before_install: 

- sudo apt-get install libeigen3-dev
- pip install --user cpp-coveralls
script:
- ./setup --enable-tests
- cd build
- make
- cd tests
- ./unit_tests.x --enable-gcov

after_success:

- coveralls --exclude lib --exclude tests --gcov-options '\-lp'
